def reverse_string(s):

    resultado = ""

    for i in range(len(s) - 1, -1, -1):
        resultado += s[i]

    return resultado

entrada = input("Digite uma string: ")
resultado_invertido = reverse_string(entrada)
print("Original:", entrada)
print("Invertida:", resultado_invertido)
